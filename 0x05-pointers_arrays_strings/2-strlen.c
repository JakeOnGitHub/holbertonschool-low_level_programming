#include "main.h"

/**
* _strlen - string length function
* @s: value passed into function
* Return: Always 0
*/

int _strlen(char *s)
{
	int strlength;

	for (strlength = 0; s[strlength] != '\0'; strlength++)
	{
		continue;
	}
	return (strlength);
}
