#include <stdio.h>
#include "main.h"
/**
* add - fuction that adds numbers
* @x: the value of 1 integer passed to the function
* @y: the value of another integer passed to the function
* Return: values passed to the console
*/
int add(int x, int y)
{
	int result;
	{
	result = x + y;
	}
	return (result);
}
