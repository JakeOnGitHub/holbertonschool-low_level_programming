#include "main.h"

/**
* print_most_numbers - function we're creating
* Return: void
*/

void print_most_numbers(void)
{
	char x;

	for (x = 0; x <= 9; x++)
	{
		if (x == 2 || x == 4)
		{
		}
		else
		{
			_putchar(x);
			x++;
		}
	{
		_putchar('\n');
	}
	}
}
