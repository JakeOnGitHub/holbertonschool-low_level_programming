#include <main.h>

/**
* _isupper - Uper C
*  @c: value passed into function
*  Return: displays output to console
*/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
}
}
