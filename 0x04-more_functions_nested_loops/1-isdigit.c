#include <main.h>
/**
* _isdigit - determines if it's a digit
* @c: is the tested input
* Return: value returned to the console
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
	{
		return (1);
	}
		else
		{
		return (0);
		}
}
